# WAM inference server environment.
#
# Reference environment:
#   Python 3.10.12, torch 2.6.0+cu126
#
# Intended scope:
#   benchmarks/common/wam_service.server_multi_gpu
#   kairos.apis / KairosEmbodiedWAMPipeline / KairosDiT / Qwen2.5-VL text encoder
#
# Suggested install order:
#   conda create -n kairos-wam-server python=3.10.12 -y
#   conda activate kairos-wam-server
#   pip install -U pip setuptools wheel ninja packaging
#   pip install -r benchmarks/common/requirment_server.txt
#   pip install flash-attn==2.6.3 --no-build-isolation
#   CUDA_HOME=/usr/local/cuda CAUSAL_CONV1D_FORCE_BUILD=TRUE CAUSAL_CONV1D_FORCE_CXX11_ABI=TRUE \
#     pip install causal-conv1d==1.5.3.post1 --no-binary causal-conv1d --no-build-isolation
#
# NVIDIA Apex is required by kairos.modules.dits.*:
#   from apex.normalization.fused_layer_norm import FusedRMSNorm
# Do not install the unrelated PyPI "apex" package. Install NVIDIA Apex from source
# or from an internal wheel built for torch 2.6 / CUDA 12.6.
#
# Example source install after torch is installed:
#   git clone https://github.com/NVIDIA/apex.git
#   cd apex
#   pip install -v --disable-pip-version-check --no-cache-dir --no-build-isolation \
#     --config-settings "--build-option=--cpp_ext" \
#     --config-settings "--build-option=--cuda_ext" .

# Use an explicit package index so this file does not depend on the user's
# global pip.conf. The default Tsinghua URL on some machines is slow/unreachable.
--index-url https://mirrors.ivolces.com/pypi/simple/
--extra-index-url https://download.pytorch.org/whl/cu126

# PyTorch / CUDA runtime
torch==2.6.0+cu126
torchvision==0.21.0+cu126
triton==3.2.0

# Build helpers for CUDA extensions
ninja==1.11.1.4
packaging==25.0
setuptools==80.9.0
wheel==0.45.1

# Core ML / model loading
transformers==5.2.0
tokenizers==0.22.1
huggingface-hub==1.8.0
safetensors==0.7.0
accelerate==1.8.1
einops==0.8.1
numpy==1.26.4
pillow==11.3.0
tqdm==4.67.1
typing_extensions==4.15.0
regex==2024.11.6
requests==2.32.4
qwen-vl-utils==0.0.14

# Kairos / WAM runtime
mmengine==0.10.7
modelscope==1.33.0
imageio==2.37.0
imageio-ffmpeg==0.6.0
av==16.0.1
termcolor==3.3.0
flash-linear-attention==0.4.0
fla-core==0.4.0

# CUDA extension: install separately after torch is installed.
# A generic prebuilt wheel can fail at import with undefined c10/cuda symbols.
# The reference env used a torch2.6/CUDA12 compatible wheel:
#   causal_conv1d-1.5.3.post1+cu12torch2.6cxx11abiTRUE-cp310-cp310-linux_x86_64.whl
# If that wheel is unavailable, build from source:
#   CUDA_HOME=/usr/local/cuda CAUSAL_CONV1D_FORCE_BUILD=TRUE CAUSAL_CONV1D_FORCE_CXX11_ABI=TRUE \
#     pip install causal-conv1d==1.5.3.post1 --no-binary causal-conv1d --no-build-isolation
# causal-conv1d==1.5.3.post1

# mmengine dependencies pinned from the reference environment
addict==2.4.0
matplotlib==3.10.3
opencv-python==4.11.0.86
PyYAML==6.0.3
rich==14.0.0
yapf==0.43.0

# HTTP server
fastapi==0.128.0
uvicorn==0.40.0
starlette==0.50.0
pydantic==2.12.5
pydantic-core==2.41.5
annotated-types==0.7.0
typing-inspection==0.4.2
anyio==4.11.0
click==8.3.1
h11==0.16.0

# Torch transitive dependencies pinned for reproducibility
filelock==3.18.0
fsspec==2025.9.0
Jinja2==3.1.6
networkx==3.4.2
sympy==1.13.1

# Optional attention acceleration in the reference environment.
# Kairos falls back to flash-attn when sageattention is unavailable.
# sageattention==20260211
